# WARM tier overrides to generate HOT promotion evidence
# Source this before running growth_supervisor

export WARM_LIVE_ENABLED=1
export WARM_MAX_BUDGET_USD=10.0
export WARM_MAX_POSITIONS=2
export ALLOW_WARM_WITHOUT_HOT=1
export WARM_TO_HOT_MIN_TRADES=10
export STRICT_HOT_GATE=0
export WARM_ADAPTIVE_GATING=1
export WARM_SPARSE_MIN_TRADES=2
export WARM_RUNTIME_NOT_HOT_ELIGIBLE_OVERRIDE=1

echo "✅ WARM tier overrides loaded - $10 budget, 2 positions max"
